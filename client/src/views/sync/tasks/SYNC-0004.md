# SYNC-0004: 字段映射器

| 属性 | 值 |
|------|-----|
| ID | SYNC-0004 |
| 类型 | DEV |
| 优先级 | P0 |
| 状态 | TODO |
| 模块 | client/src |
| 依赖 | SYNC-0001 |

---

## 1. 目标

将 Lua 缩写字段映射为完整字段名，转换为 TypeScript 类型。

**字段对照表**: PRD2 - 5.1 TRP3字段对照表

---

## 2. 接口设计

```typescript
// utils/fieldMapper.ts

export function mapProfile(raw: unknown): Profile;
export function mapCharacteristics(raw: unknown): Characteristics;
export function mapAbout(raw: unknown): About;

// 反向映射（编辑后写回）
export function unmapProfile(profile: Profile): unknown;
```

---

## 3. 验收标准

- [ ] 映射所有 characteristics 字段
- [ ] 映射 about 三种模板
- [ ] 映射 PS 性格特征（预设+自定义）
- [ ] 映射 MI 其他信息
- [ ] 处理缺失字段默认值
- [ ] 支持反向映射
